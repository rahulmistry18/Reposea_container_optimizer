# RepoSea — Python Dependencies

# Core data processing
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0

# Excel export (Power BI / Tableau formatted workbook)
openpyxl>=3.1.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Code quality
ruff>=0.4.0

# Live data sources (all optional — pipeline degrades gracefully if unavailable)
# urllib (stdlib) used for HTTP — no extra dependency needed

# Optional: set in GitHub Actions secrets for live weather data
# OPENWEATHER_API_KEY=<your free key from openweathermap.org>
# Free tier: 1000 calls/day — pipeline only calls ~4 ports per run
